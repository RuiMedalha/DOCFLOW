-- Placeholder. Original SQL was wiped when working directories were rebuilt.
-- The schema was already applied to the live DB; this placeholder satisfies
-- `prisma migrate resolve` so the chain stays intact. Never run this on a
-- fresh database.

SELECT 1;
